info:
  name: sync-catalog
  type: http
  seq: 2

http:
  method: post
  url: "{{api_url}}/cars/sync-catalog"
  auth: inherit

runtime:
  variables:
    - name: api_url
      value: http://localhost:8005

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
