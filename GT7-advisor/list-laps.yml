info:
  name: list-laps
  type: http
  seq: 11

http:
  method: get
  url: "{{api_url}}/sessions/2/laps"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
