info:
  name: health
  type: http
  seq: 1

http:
  method: GET
  url: "{{api_url}}/health"
  auth: inherit

runtime:
  variables:
    - name: api_url
      value: http://localhost:8005

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
