info:
  name: activate-session
  type: http
  seq: 7

http:
  method: post
  url: "{{api_url}}/sessions/1/activate"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
