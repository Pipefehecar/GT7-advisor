info:
  name: request-suggestion
  type: http
  seq: 12

http:
  method: post
  url: "{{api_url}}/sessions/2/suggest"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
